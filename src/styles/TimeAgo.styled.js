import styled from "styled-components";

export const STimeAgo = styled.span`//CHANGE možda obrisati sve
    position: absolute;
    bottom: 0.25rem;
    right: 0.25rem;
    font-size: 0.75rem;
   
    //background-color: red;

    
    @media(min-width: ${({theme}) => theme.size.tablet}) {
        
    }
`